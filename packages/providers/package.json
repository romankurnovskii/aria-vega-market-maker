{
  "name": "@lp-system/providers",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@lp-system/core": "workspace:*",
    "@solana/web3.js": "^1.98.4"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "node --test src/**/*.test.ts",
    "test:coverage": "node --experimental-test-coverage --test src/**/*.test.ts",
    "test:watch": "nodemon --exec \"npm test\" --watch src"
  }
}
