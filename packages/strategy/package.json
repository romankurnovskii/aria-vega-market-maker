{
  "name": "@lp-system/strategy",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@lp-system/core": "workspace:*",
    "@lp-system/logger": "workspace:*",
    "@lp-system/steps": "workspace:*"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "tsx --test src/**/*.test.ts",
    "test:coverage": "tsx --test --experimental-test-coverage src/**/*.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.1"
  }
}
