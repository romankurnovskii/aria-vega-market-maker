/**
 * @file PoolDetail.tsx
 * @description Displays detailed pool information for a given pool address within a wallet context.
 *
 * @features
 * - Shows pool address, token pair, fee tier, and bin step
 * - Renders position summary stats for the pool
 *
 * @dependencies wallet types, format utils
 */
